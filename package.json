{
  "name": "taskflow",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "repository": { "type": "git", "url": "git+https://github.com/freeappstore-online/taskflow.git" },
  "scripts": {
    "dev": "pnpm --filter @taskflow/web dev",
    "build": "pnpm --filter @taskflow/web build",
    "preview": "pnpm --filter @taskflow/web preview",
    "typecheck": "pnpm --filter @taskflow/web exec tsc -b",
    "test": "pnpm --filter @taskflow/web exec tsc -b"
  }
}
